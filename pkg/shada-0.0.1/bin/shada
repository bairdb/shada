#!/usr/bin/ruby

ENV['CONFIG'] = "main"
ENV['ROOT'] = '/home/admin/base/site/'

require "shada"

Shada::CLI.cli do
  
	task :generate_module do |args|
		puts args
	end
	
	task :generate_model do |args|
		puts args
	end
	
	task :generate_controller do |args|
		puts args
	end
	
	task :create_table do |args|
	  puts args
  end
	
end

